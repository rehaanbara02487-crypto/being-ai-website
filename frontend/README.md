# BeingAI Frontend

Next.js + Tailwind + TypeScript UI.

**Phase 0:** Scaffold only (not yet initialized).

```bash
npx create-next-app@latest . --typescript --tailwind --app --src-dir
```

**Planned pages:**

- `/` — New project prompt
- `/projects/[id]` — Chat, plan viewer, terminal stream, file tree
- `/settings` — LLM model, GitHub, safety toggles
